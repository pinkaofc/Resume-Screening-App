# Pinned to the versions the pickled model was trained with.
# clf.pkl / tfidf.pkl were serialised by scikit-learn 1.7.1 — changing this
# pin produces InconsistentVersionWarning and can silently alter predictions.
streamlit==1.48.1
scikit-learn==1.7.1
numpy==2.2.6
scipy==1.15.3
pypdf==5.1.0
